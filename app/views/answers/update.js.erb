$('.answer[data-answer-id=<%= answer.id %>] .answer-edit-errors').html('<%= render 'shared/errors', resource: answer %>');

<% unless answer.errors.present? %> 
  $('.answer[data-answer-id=<%= answer.id %>]').replaceWith('<%= j render answer %>');
<% end %>
